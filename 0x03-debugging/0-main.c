#include "main.h"

/**
 * main - tests function
 * that prints if integer
 * is positive or negative
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	/* variable declaration */
	int i;

	/* variable initialization */
	i = 0;

	/* function call */
	positive_or_negative(i);

	return (0);
}
